# auth

This library contains authentication and role-based access control (RBAC) logic.

## Features

- JWT authentication guard
- Permission service for role hierarchy
- Role-based access control guard
- Audit interceptor for logging requests
- Require role decorator for route protection

## Running tests

```bash
nx test auth
```
